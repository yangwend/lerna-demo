{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "clean:dist": "lerna run clean:dist",
    "clean": "lerna run clean && rimraf node_modules"
  },
  "devDependencies": {
    "lerna": "^5.1.2"
  }
}
